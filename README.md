## AI for Text Adventures

This is the repository for the project for AI for Games course.

Use `env.sh` to set environment.

Install `mongodb` to use spearmint.

Run `massRuner.sh` to run agent on provided games.
